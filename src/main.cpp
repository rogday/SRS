#include <cstdint>
#include <iostream>

#include <boost/graph/depth_first_search.hpp>

#include <Graph.h>

int main() {
	std::uint64_t n, m;
	std::cout << "n, m = " << std::endl;
	std::cin >> n >> m;
	
}